{
  "name": "dataquest",
  "version": "1.0.0",
  "description": "DataQuest — Data Analytics Learning Platform",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
